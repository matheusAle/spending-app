type Spending {
    paymentMethoad: String!
}

input SpendingInput {
    spending: SpendingInput
}

type Query {
    spendings: [Spending] @auth
}

type Mutation {
    createSpending(spending: SpendingInput!): Spending @auth
}
